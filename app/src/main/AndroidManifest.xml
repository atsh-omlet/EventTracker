<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
>

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventTrackerAtsushi"
        tools:targetApi="31">

        <receiver
            android:name="com.atsushi.event_tracker.util.NotificationReceiver"
            android:exported="true"
            android:enabled="true"
            android:stopWithTask="false">
        </receiver>

        <receiver
            android:name="com.atsushi.event_tracker.util.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.atsushi.event_tracker.ui.EventDetailsActivity"
            android:exported="false" />
        <activity
            android:name="com.atsushi.event_tracker.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.atsushi.event_tracker.ui.DashboardActivity" />
        <activity android:name="com.atsushi.event_tracker.ui.SettingsActivity" />
        <activity android:name="com.atsushi.event_tracker.ui.CreateLoginActivity" />
        <activity android:name="com.atsushi.event_tracker.ui.ConfirmPasswordActivity" />
        <activity android:name="com.atsushi.event_tracker.ui.NewPasswordActivity" />



    </application>


</manifest>